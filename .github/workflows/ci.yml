name: CI

# This workflow is kept for organizational purposes
# Individual workflows run on their own triggers (push/PR)
# This ensures all checks run even if paths don't match
on:
  workflow_dispatch:

jobs:
  # Jobs are defined in individual workflow files
  # They run automatically on push/PR via their own triggers
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: CI Status
        run: echo "✅ Individual workflows run on push/PR automatically"
